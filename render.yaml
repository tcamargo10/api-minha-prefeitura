services:
  - type: web
    name: api-minha-prefeitura
    runtime: node
    buildCommand: npm run build:render
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
